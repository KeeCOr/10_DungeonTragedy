{
  "name": "dragon-tactics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --test-reporter=spec tests/**/*.test.js",
    "serve": "python3 -m http.server 8000"
  }
}
